{
  "name": "bitbucket-cli",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && webpack",
    "start": "npm run build && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^7.0.1"
  },
  "bin": {
    "bitbucket_cli": "src/index.js"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "webpack": "^4.41.4",
    "webpack-cli": "^3.3.10"
  }
}
